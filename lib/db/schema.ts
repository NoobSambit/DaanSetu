import { bigint, boolean, date, doublePrecision, index, integer, jsonb, pgEnum, pgTable, text, timestamp, uniqueIndex, uuid } from 'drizzle-orm/pg-core'

export const role = pgEnum('user_role', ['supporter', 'ngo', 'corporate', 'admin'])
export const campaignStatus = pgEnum('campaign_status', ['draft', 'pending_review', 'changes_requested', 'rejected', 'approved', 'active', 'paused', 'completed', 'cancelled'])
export const verificationStatus = pgEnum('verification_status', ['draft', 'submitted', 'changes_requested', 'verified', 'rejected', 'expired'])

const timestamps = { createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull(), updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull() }
export const users = pgTable('users', { id: uuid('id').defaultRandom().primaryKey(), name: text('name').notNull(), email: text('email').notNull(), emailVerified: boolean('email_verified').default(false).notNull(), image: text('image'), role: role('role').default('supporter').notNull(), ...timestamps }, (t) => [uniqueIndex('users_email_unique').on(t.email)])
export const sessions = pgTable('sessions', { id: text('id').primaryKey(), userId: uuid('user_id').references(() => users.id, { onDelete: 'cascade' }).notNull(), token: text('token').notNull(), expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(), ipAddress: text('ip_address'), userAgent: text('user_agent'), ...timestamps }, (t) => [uniqueIndex('sessions_token_unique').on(t.token), index('sessions_user_idx').on(t.userId)])
export const accounts = pgTable('accounts', { id: text('id').primaryKey(), userId: uuid('user_id').references(() => users.id, { onDelete: 'cascade' }).notNull(), accountId: text('account_id').notNull(), providerId: text('provider_id').notNull(), password: text('password'), accessToken: text('access_token'), refreshToken: text('refresh_token'), accessTokenExpiresAt: timestamp('access_token_expires_at', { withTimezone: true }), refreshTokenExpiresAt: timestamp('refresh_token_expires_at', { withTimezone: true }), scope: text('scope'), idToken: text('id_token'), ...timestamps })
export const verifications = pgTable('verifications', { id: text('id').primaryKey(), identifier: text('identifier').notNull(), value: text('value').notNull(), expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(), ...timestamps })
export const ngoProfiles = pgTable('ngo_profiles', { id: uuid('id').defaultRandom().primaryKey(), ownerId: uuid('owner_id').references(() => users.id).notNull(), displayName: text('display_name').notNull(), slug: text('slug').notNull(), description: text('description'), city: text('city'), state: text('state'), latitude: doublePrecision('latitude'), longitude: doublePrecision('longitude'), categories: text('categories').array().default([]).notNull(), verificationStatus: verificationStatus('verification_status').default('draft').notNull(), isDiscoverable: boolean('is_discoverable').default(false).notNull(), is80gEligible: boolean('is_80g_eligible').default(false).notNull(), ...timestamps }, (t) => [uniqueIndex('ngo_profiles_slug_unique').on(t.slug)])
export const payoutAccounts = pgTable('payout_accounts', { id: uuid('id').defaultRandom().primaryKey(), ownerId: uuid('owner_id').references(() => users.id).notNull(), provider: text('provider').notNull(), gatewayAccountId: text('gateway_account_id'), status: text('status').default('pending').notNull(), beneficiary: jsonb('beneficiary'), ...timestamps })
export const campaigns = pgTable('campaigns', { id: uuid('id').defaultRandom().primaryKey(), creatorId: uuid('creator_id').references(() => users.id).notNull(), ngoId: uuid('ngo_id').references(() => ngoProfiles.id), payoutAccountId: uuid('payout_account_id').references(() => payoutAccounts.id), title: text('title').notNull(), shortDescription: text('short_description'), story: text('story').notNull(), category: text('category').default('general').notNull(), imageUrl: text('image_url'), deadline: timestamp('deadline', { withTimezone: true }), beneficiary: jsonb('beneficiary'), evidence: jsonb('evidence').default([]).notNull(), targetPaise: bigint('target_paise', { mode: 'number' }).notNull(), status: campaignStatus('status').default('draft').notNull(), approvedAt: timestamp('approved_at', { withTimezone: true }), ...timestamps }, (t) => [index('campaigns_status_idx').on(t.status)])
export const donations = pgTable('donations', { id: uuid('id').defaultRandom().primaryKey(), donorId: uuid('donor_id').references(() => users.id).notNull(), campaignId: uuid('campaign_id').references(() => campaigns.id).notNull(), amountPaise: bigint('amount_paise', { mode: 'number' }).notNull(), gatewayOrderId: text('gateway_order_id').notNull(), gatewayPaymentId: text('gateway_payment_id').notNull(), status: text('status').notNull(), capturedAt: timestamp('captured_at', { withTimezone: true }).notNull(), ...timestamps }, (t) => [uniqueIndex('donations_order_unique').on(t.gatewayOrderId), uniqueIndex('donations_payment_unique').on(t.gatewayPaymentId)])
export const paymentEvents = pgTable('payment_events', { id: uuid('id').defaultRandom().primaryKey(), gatewayEventId: text('gateway_event_id').notNull(), eventType: text('event_type').notNull(), payload: jsonb('payload').notNull(), processedAt: timestamp('processed_at', { withTimezone: true }), createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull() }, (t) => [uniqueIndex('payment_events_gateway_unique').on(t.gatewayEventId)])
export const volunteerApplications = pgTable('volunteer_applications', { id: uuid('id').defaultRandom().primaryKey(), userId: uuid('user_id').references(() => users.id).notNull(), ngoId: uuid('ngo_id').references(() => ngoProfiles.id).notNull(), opportunityId:uuid('opportunity_id'),message:text('message'), status: text('status').default('submitted').notNull(), ...timestamps })
export const posts = pgTable('posts', { id: uuid('id').defaultRandom().primaryKey(), authorId: uuid('author_id').references(() => users.id).notNull(), title:text('title'), body: text('body').notNull(),media:jsonb('media').default([]).notNull(),status:text('status').default('published').notNull(),isImpactStory:boolean('is_impact_story').default(false).notNull(),featuredAt:timestamp('featured_at',{withTimezone:true}), approvedAt: timestamp('approved_at', { withTimezone: true }), hiddenAt: timestamp('hidden_at', { withTimezone: true }), ...timestamps })
export const auditLogs = pgTable('audit_logs', { id: uuid('id').defaultRandom().primaryKey(), actorId: uuid('actor_id').references(() => users.id), action: text('action').notNull(), entityType: text('entity_type').notNull(), entityId: text('entity_id'), metadata: jsonb('metadata'), createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull() })
export const reviews = pgTable('reviews', { id: uuid('id').defaultRandom().primaryKey(), userId: uuid('user_id').references(() => users.id).notNull(), ngoId: uuid('ngo_id').references(() => ngoProfiles.id).notNull(), rating: integer('rating').notNull(), body: text('body'), hiddenAt: timestamp('hidden_at', { withTimezone: true }), createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull() }, (t) => [uniqueIndex('reviews_user_ngo_unique').on(t.userId, t.ngoId)])
export const campaignUpdates = pgTable('campaign_updates', { id: uuid('id').defaultRandom().primaryKey(), campaignId: uuid('campaign_id').references(() => campaigns.id, { onDelete: 'cascade' }).notNull(), authorId: uuid('author_id').references(() => users.id).notNull(), title: text('title').notNull(), body: text('body').notNull(), publishedAt: timestamp('published_at', { withTimezone: true }), ...timestamps })
export const follows = pgTable('follows', { id: uuid('id').defaultRandom().primaryKey(), userId: uuid('user_id').references(() => users.id, { onDelete: 'cascade' }).notNull(), ngoId: uuid('ngo_id').references(() => ngoProfiles.id, { onDelete: 'cascade' }).notNull(), createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull() }, (t) => [uniqueIndex('follows_user_ngo_unique').on(t.userId, t.ngoId)])
export const paymentOrders = pgTable('payment_orders', { id: uuid('id').defaultRandom().primaryKey(), userId: uuid('user_id').references(() => users.id).notNull(), campaignId: uuid('campaign_id').references(() => campaigns.id).notNull(), amountPaise: bigint('amount_paise', { mode: 'number' }).notNull(), gatewayOrderId: text('gateway_order_id').notNull(), status: text('status').default('created').notNull(), expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(), ...timestamps }, (t) => [uniqueIndex('payment_orders_gateway_unique').on(t.gatewayOrderId)])
export const subscriptions = pgTable('subscriptions', { id: uuid('id').defaultRandom().primaryKey(), userId: uuid('user_id').references(() => users.id).notNull(), campaignId: uuid('campaign_id').references(() => campaigns.id).notNull(), amountPaise: bigint('amount_paise', { mode: 'number' }).notNull(), cadence: text('cadence').notNull(), gatewayPlanId: text('gateway_plan_id').notNull(), gatewaySubscriptionId: text('gateway_subscription_id').notNull(), status: text('status').default('created').notNull(), currentPeriodEnd: timestamp('current_period_end', { withTimezone: true }), ...timestamps }, (t) => [uniqueIndex('subscriptions_gateway_unique').on(t.gatewaySubscriptionId)])
export const refunds = pgTable('refunds', { id: uuid('id').defaultRandom().primaryKey(), donationId: uuid('donation_id').references(() => donations.id).notNull(), requesterId: uuid('requester_id').references(() => users.id).notNull(), reason: text('reason').notNull(), status: text('status').default('requested').notNull(), gatewayRefundId: text('gateway_refund_id'), reviewedBy: uuid('reviewed_by').references(() => users.id), reviewedAt: timestamp('reviewed_at', { withTimezone: true }), ...timestamps }, (t) => [uniqueIndex('refunds_gateway_unique').on(t.gatewayRefundId)])
export const receipts = pgTable('receipts', { id: uuid('id').defaultRandom().primaryKey(), donationId: uuid('donation_id').references(() => donations.id).notNull(), receiptNumber: text('receipt_number').notNull(), financialYear: text('financial_year').notNull(), issuedAt: timestamp('issued_at', { withTimezone: true }).defaultNow().notNull() }, (t) => [uniqueIndex('receipts_donation_unique').on(t.donationId), uniqueIndex('receipts_number_unique').on(t.receiptNumber)])
export const taxCertificates = pgTable('tax_certificates', { id: uuid('id').defaultRandom().primaryKey(), donationId: uuid('donation_id').references(() => donations.id).notNull(), ngoId: uuid('ngo_id').references(() => ngoProfiles.id).notNull(), financialYear: text('financial_year').notNull(), storageKey: text('storage_key').notNull(), uploadedBy: uuid('uploaded_by').references(() => users.id).notNull(), issuedAt: date('issued_at').notNull(), createdAt: timestamp('created_at', { withTimezone: true }).defaultNow().notNull() })
export const payoutTransfers = pgTable('payout_transfers', { id: uuid('id').defaultRandom().primaryKey(), payoutAccountId: uuid('payout_account_id').references(() => payoutAccounts.id).notNull(), donationId: uuid('donation_id').references(() => donations.id), amountPaise: bigint('amount_paise', { mode: 'number' }).notNull(), gatewayTransferId: text('gateway_transfer_id'), status: text('status').default('pending').notNull(), failureReason: text('failure_reason'), reversedAt: timestamp('reversed_at', { withTimezone: true }), ...timestamps })
export const volunteerProfiles = pgTable('volunteer_profiles', { userId: uuid('user_id').references(() => users.id, {onDelete:'cascade'}).primaryKey(), bio:text('bio'), skills:text('skills').array().default([]).notNull(), city:text('city'), state:text('state'), availability:text('availability').array().default([]).notNull(), remoteOk:boolean('remote_ok').default(false).notNull(), ...timestamps })
export const volunteerOpportunities = pgTable('volunteer_opportunities', { id:uuid('id').defaultRandom().primaryKey(), ngoId:uuid('ngo_id').references(()=>ngoProfiles.id).notNull(), title:text('title').notNull(), description:text('description').notNull(), skills:text('skills').array().default([]).notNull(), city:text('city'), state:text('state'), remote:boolean('remote').default(false).notNull(), availability:text('availability').array().default([]).notNull(), capacity:integer('capacity').notNull(), status:text('status').default('draft').notNull(), startsAt:timestamp('starts_at',{withTimezone:true}), endsAt:timestamp('ends_at',{withTimezone:true}), ...timestamps })
export const volunteerHours=pgTable('volunteer_hours',{id:uuid('id').defaultRandom().primaryKey(),applicationId:uuid('application_id').references(()=>volunteerApplications.id).notNull(),minutes:integer('minutes').notNull(),serviceDate:date('service_date').notNull(),description:text('description').notNull(),status:text('status').default('pending').notNull(),reviewedBy:uuid('reviewed_by').references(()=>users.id),reviewedAt:timestamp('reviewed_at',{withTimezone:true}),...timestamps})
export const volunteerCertificates=pgTable('volunteer_certificates',{id:uuid('id').defaultRandom().primaryKey(),userId:uuid('user_id').references(()=>users.id).notNull(),ngoId:uuid('ngo_id').references(()=>ngoProfiles.id).notNull(),certificateNumber:text('certificate_number').notNull(),approvedMinutes:integer('approved_minutes').notNull(),issuedAt:timestamp('issued_at',{withTimezone:true}).defaultNow().notNull()},t=>[uniqueIndex('volunteer_certificate_number_unique').on(t.certificateNumber)])
export const postLikes=pgTable('post_likes',{userId:uuid('user_id').references(()=>users.id).notNull(),postId:uuid('post_id').references(()=>posts.id,{onDelete:'cascade'}).notNull(),createdAt:timestamp('created_at',{withTimezone:true}).defaultNow().notNull()},t=>[uniqueIndex('post_likes_once').on(t.userId,t.postId)])
export const postComments=pgTable('post_comments',{id:uuid('id').defaultRandom().primaryKey(),userId:uuid('user_id').references(()=>users.id).notNull(),postId:uuid('post_id').references(()=>posts.id,{onDelete:'cascade'}).notNull(),body:text('body').notNull(),hiddenAt:timestamp('hidden_at',{withTimezone:true}),...timestamps})
export const postBookmarks=pgTable('post_bookmarks',{userId:uuid('user_id').references(()=>users.id).notNull(),postId:uuid('post_id').references(()=>posts.id,{onDelete:'cascade'}).notNull(),createdAt:timestamp('created_at',{withTimezone:true}).defaultNow().notNull()},t=>[uniqueIndex('post_bookmarks_once').on(t.userId,t.postId)])
export const reports=pgTable('reports',{id:uuid('id').defaultRandom().primaryKey(),reporterId:uuid('reporter_id').references(()=>users.id).notNull(),entityType:text('entity_type').notNull(),entityId:uuid('entity_id').notNull(),reason:text('reason').notNull(),status:text('status').default('open').notNull(),reviewedBy:uuid('reviewed_by').references(()=>users.id),reviewedAt:timestamp('reviewed_at',{withTimezone:true}),createdAt:timestamp('created_at',{withTimezone:true}).defaultNow().notNull()})
export const notifications=pgTable('notifications',{id:uuid('id').defaultRandom().primaryKey(),userId:uuid('user_id').references(()=>users.id,{onDelete:'cascade'}).notNull(),type:text('type').notNull(),title:text('title').notNull(),body:text('body').notNull(),href:text('href'),readAt:timestamp('read_at',{withTimezone:true}),createdAt:timestamp('created_at',{withTimezone:true}).defaultNow().notNull()})
export const realtimeEvents=pgTable('realtime_events',{id:bigint('id',{mode:'number'}).primaryKey().generatedAlwaysAsIdentity(),audienceUserId:uuid('audience_user_id').references(()=>users.id),topic:text('topic').notNull(),payload:jsonb('payload').notNull(),createdAt:timestamp('created_at',{withTimezone:true}).defaultNow().notNull()})
