import { getPublicImpact } from '@/lib/repositories/impact'
import { formatPaise } from '@/lib/domain/finance'
export const dynamic='force-dynamic'
export default async function PublicAnalyticsPage(){const data=await getPublicImpact();return <main className="min-h-screen bg-slate-50"><div className="mx-auto max-w-6xl px-4 py-10"><h1 className="text-4xl font-bold">Public impact</h1><p className="mt-2 text-slate-600">Platform-tracked totals; NGO-reported outcomes are shown only on their attributed profiles and stories.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{[['Captured funds',formatPaise(data.fundsPaise)],['Transactions',data.donationCount],['Active campaigns',data.activeCampaigns],['Verified NGOs',data.verifiedNgos],['States',data.statesReached]].map(([label,value])=><div key={label} className="rounded-2xl border bg-white p-6"><p className="text-sm text-slate-500">{label}</p><strong className="mt-2 block text-2xl">{value}</strong></div>)}</div></div></main>}
