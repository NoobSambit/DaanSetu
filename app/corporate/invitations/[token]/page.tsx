import { acceptInvitationAction } from './actions';export default async function InvitationPage({params}:{params:Promise<{token:string}>}){const{token}=await params;return <main className="mx-auto max-w-xl p-10"><h1 className="text-3xl font-bold">Corporate employee invitation</h1><p className="mt-3 text-slate-600">Sign in with the invited, verified email address, then accept to link your future eligible gifts.</p><form action={acceptInvitationAction} className="mt-6"><input type="hidden" name="token" value={token}/><button className="rounded bg-blue-600 px-5 py-3 font-bold text-white">Accept invitation</button></form></main>}
