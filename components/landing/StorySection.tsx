'use client'
import Link from 'next/link'
import { motion } from 'framer-motion'
import { ArrowRight, BadgeCheck, FileCheck2, ShieldCheck } from 'lucide-react'

export default function StorySection() {
  return <section className="bg-white py-20"><div className="container-custom"><div className="grid gap-6 lg:grid-cols-4"><motion.div initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className="rounded-[24px] border border-slate-200 bg-white p-7 shadow-sm lg:col-span-2"><p className="text-xs font-bold uppercase tracking-widest text-blue-600">Impact stories</p><h2 className="mt-3 font-serif text-3xl font-bold text-slate-900">Stories appear only after moderation</h2><p className="mt-4 leading-7 text-slate-600">DaanSetu does not invent beneficiary quotes or impact totals. Approved organizations and community members publish evidence-backed updates, and featured stories are selected explicitly.</p><Link href="/impact-stories" className="mt-7 inline-flex items-center gap-2 rounded-xl border border-blue-200 px-5 py-3 text-sm font-bold text-blue-700">Browse approved stories <ArrowRight className="h-4 w-4"/></Link></motion.div>{[{icon:BadgeCheck,title:'Verified source',text:'The author and linked organization remain visible.'},{icon:FileCheck2,title:'Measured outcomes',text:'Reported impact is kept distinct from platform-tracked activity.'},{icon:ShieldCheck,title:'Moderated records',text:'Reports can hide unsafe content without erasing audit history.'}].slice(0,2).map(item => <div key={item.title} className="rounded-[24px] bg-[#fef9f3] p-7"><item.icon className="h-7 w-7 text-amber-700"/><h3 className="mt-5 text-lg font-bold text-slate-900">{item.title}</h3><p className="mt-3 text-sm leading-6 text-slate-600">{item.text}</p></div>)}</div></div></section>
}
